# Production env for GitHub Pages (static hosting)
# ONLY VITE_ vars are embedded into the JS bundle — safe to commit (publishable key is public)
# NEVER put sb_secret_… or DATABASE_URL here — those are server-only

VITE_SUPABASE_URL=https://mlncmwgoljergzncycsa.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_nkwzs0o0B1vsULUZUiaqJg_9qMiZQRW
# For GitHub Pages there is NO Express server, so disable Prisma API and use direct Supabase
# Leave empty to skip /api/visitors and fallback to supabase-js directly (works on static hosting)
VITE_VISITOR_ENDPOINT=
